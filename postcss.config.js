// Tailwind v3's PostCSS plugin shape. Upgrading to v4's `@tailwindcss/postcss`
// is a separate migration (new config format) -- not part of this project yet.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
